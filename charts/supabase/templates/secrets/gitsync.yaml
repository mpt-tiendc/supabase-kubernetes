{{- if .Values.secret.gitSync }}
{{- if not .Values.secret.gitSync.secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supabase.secret.gitSync" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value := .Values.secret.gitSync }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | toString | b64enc }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
